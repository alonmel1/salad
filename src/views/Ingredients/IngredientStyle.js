import styled from "@emotion/styled";

export const IngredientContainer = styled.div`
  user-select: none;
  display: flex;
  flex-direction: column;

  .total-price {
    height: 30px;
    display: block;
    font-weight: bold;
  }
`
