import React from 'react';
import PropTypes from 'prop-types';

const Summary = props => {
    return (
        <div>
            This is Sum
        </div>
    );
};

Summary.propTypes = {

};

export default Summary;
